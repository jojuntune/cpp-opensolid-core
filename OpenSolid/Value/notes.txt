---------- ValueBase ----------

BlockReference
BlockIterator -> BlockReference
BlockRange -> BlockIterator, BlockIterator

begin, end
rowwise (begin, end)
colwise (begin, end)
x
y
z
w
operator()(index)
operator()(row, col)
operator()(First(num))
operator()(Last(num))
operator()(Range(start, num))
operator()(All(), col) // col()
operator()(row, All()) // row()
operator()(First(num), col)

---------- Double / Interval / MatrixBase ----------

data
lower
upper
median
width
cwiseLower
cwiseUpper
cwiseWidth
cwiseMedian
cwiseProduct
cwiseQuotient
unaryExpr
binaryExpr
dot
cross
inverse
determinant
norm
normalized
bounds
hashValue
transposedProduct
Constant
LinSpaced
Ones
Zero
Identity
Unit

---------- Double / Interval / MatrixBase / ValueBase (built-in type overloads) ----------
isZero
isIdentity
isOrthogonal
isEqualTo
isParallelTo
isPerpendicularTo
isSubsetOf
isProperSubsetOf
isSupersetOf
isProperSupersetOf

---------- Global ----------
==, !=
unary -, +, -, *
abs
sqrt
etc.

---------- Names ----------

Double
Interval

Matrix<Double>
Vector<Double>
RowVector<Double>

Matrix<Interval>
Vector<Interval>
RowVector<Interval>

In Python, etc.: simply Matrix/Vector/RowVector, construct C++ object of type appropriate for
given constructor arguments

